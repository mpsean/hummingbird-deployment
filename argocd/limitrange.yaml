apiVersion: v1
kind: LimitRange
metadata:
  name: argocd-defaults
  namespace: argocd
spec:
  limits:
    - type: Container
      default:
        cpu: 300m
        memory: 512Mi
      defaultRequest:
        cpu: 50m
        memory: 64Mi
